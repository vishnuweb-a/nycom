/**
 * Four-step checkout: address, delivery method, payment method and order summary. Arrives in Phase 8.
 */
const CheckoutPage = () => (
  <div className="container-page py-12">
    <h1 className="text-h3 md:text-h1">Checkout</h1>
  </div>
);

export default CheckoutPage;
