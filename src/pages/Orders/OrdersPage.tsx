/**
 * Order history with status, tracking, cancellation, invoice and reorder actions. Arrives in Phase 9.
 */
const OrdersPage = () => (
  <div className="container-page py-12">
    <h1 className="text-h3 md:text-h1">My Orders</h1>
  </div>
);

export default OrdersPage;
