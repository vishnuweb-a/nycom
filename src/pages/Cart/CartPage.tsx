/**
 * Shopping cart. Line items, quantity controls, the coupon field and the order summary arrive in Phase 7.
 */
const CartPage = () => (
  <div className="container-page py-12">
    <h1 className="text-h3 md:text-h1">Your Cart</h1>
  </div>
);

export default CartPage;
