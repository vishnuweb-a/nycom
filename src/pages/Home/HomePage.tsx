/**
 * Landing page. Hero carousel, categories, featured and top-selling products, benefits, newsletter and the contact form arrive in Phase 4.
 */
const HomePage = () => (
  <div className="container-page py-12">
    <h1 className="text-h3 md:text-h1">Yarnvia</h1>
  </div>
);

export default HomePage;
