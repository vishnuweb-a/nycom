/**
 * Product browsing. Search, the filter sidebar, sorting, the responsive grid and pagination arrive in Phase 5.
 */
const ShopPage = () => (
  <div className="container-page py-12">
    <h1 className="text-h3 md:text-h1">Shop</h1>
  </div>
);

export default ShopPage;
