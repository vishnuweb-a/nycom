/**
 * URL parameter names for the Shop listing.
 *
 * Search, filter and sort state lives in the query string so results are
 * shareable, bookmarkable and survive the back button. Every producer and
 * consumer of that state reads these constants — never a literal.
 */

export const SEARCH_QUERY_PARAM = 'q';
